# Mock Images

Placeholder images used by `src/mocks/*` fixtures during the
`sef-ui-mock-preview` change. Served by Vite from `/mock-images/*`
URLs in both dev and production builds.

Expected files:
- `avatar-default.png` — default user avatar (square, ~120px)
- `chat-image-1.jpg`, `chat-image-2.jpg`, `chat-image-3.jpg` — sample chat photos (any aspect)
- `sticker-1.png` .. `sticker-5.png` — custom sticker placeholders (square, ~80px, transparent bg)
- `donate-banner.jpg` — donation banner placeholder (any aspect)

Replace with real assets when wiring the backend.
